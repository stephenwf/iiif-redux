export function getLabel(manifest) {
  return manifest.label;
}

export function getAttribution(manifest) {
  return manifest.attribution;
}

export function getMetadata(manifest) {
  return manifest.metadata;
}
